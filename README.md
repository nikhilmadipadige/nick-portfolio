there are a lot of unnecessary files in the repo, since i tried building different themes, but chose one theme in the end.
the important files are index.html, styles-modern.css
The old "Stark" theme files are still in the repo (renamed to -legacy) just in case to reference them.
